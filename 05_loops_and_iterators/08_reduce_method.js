// Reduce Method :- executes an user supplied reducer
// At last returns a single value
/*
Syntax :-

    arr_name.reduce(callback_func(x,y){
        return ...
    } , z)

    x = accumulator
    y = current element value from array
    z = initial value
    
*/
